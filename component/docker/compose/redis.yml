    {{ container.name }}:
        container_name: {{ container.name }}
        image: redis:alpine
        restart: always
        volumes:
            - {{ storage }}:/data
        ports:
            - {{ port }}:6379
